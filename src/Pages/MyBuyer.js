import React from 'react';
import useTitle from '../Hooks/useTitle';

const MyBuyer = () => {
    useTitle('My Buyer');
    return (
        <div>
            my buyer
        </div>
    );
};

export default MyBuyer;